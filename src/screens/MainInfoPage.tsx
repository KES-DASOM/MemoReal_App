import React from 'react';
import { View, Text, Image, ScrollView, TouchableOpacity} from 'react-native';

export default function MainInfoPage() {

  return (
    <ScrollView className="">
      <Text>메모리얼 알아보기 페이지</Text>
    </ScrollView>
  );
}
